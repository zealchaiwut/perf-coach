fastapi==0.136.3
uvicorn==0.48.0
psycopg2-binary==2.9.12
sqlalchemy==2.0.50
python-dotenv==1.2.2
alembic==1.18.4
cryptography==48.0.0
requests==2.32.3
python-multipart==0.0.20
scipy==1.18.0
httpx

# Test/dev dependencies are NOT here — see requirements-dev.txt. Render installs
# this file into a 512MB instance with an OOM history (PR #1576), so it carries
# only what serving traffic needs.
#
# Removed 2026-07-31: langgraph and pydantic-ai-slim. They existed for the
# PLAN_ORCH=langgraph|pydantic_ai plan-suggestion orchestrators, which #1597
# deleted once the A/B comparison ended. pydantic-ai had zero remaining imports;
# langgraph's only consumer was the PARKED coach_orch_langgraph, whose imports
# are function-local inside a try/except that falls back to the deterministic
# path. If that module is ever un-parked rather than deleted, langgraph comes
# back with it.
